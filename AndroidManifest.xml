<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mentalhealthproject"
    android:versionCode="1"
    android:versionName="1.0">

    <uses-sdk
        android:minSdkVersion="19"
        android:targetSdkVersion="19" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Splash Screen Activity (Only this should have LAUNCHER intent filter) -->
        <activity android:name=".SplashActivity"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity (No intent filter needed) -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />

        <!-- Login Activity (Register it here) -->
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name" />

        <!-- Signup Activity (Register it here) -->
        <activity
            android:name=".SignupActivity"
            android:label="@string/app_name" />

        <!-- Home Activity (Register it here) -->
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name" />
        
           <!-- Home Activity (Register it here) -->
        <activity
            android:name=".BreatheActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".JournalActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MoodTrackerActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MotivationActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".MoveActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".ProductsActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".RoutinesActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".SleepActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".UserActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".WakeupActivity"
            android:label="@string/app_name" />
           <activity
            android:name=".FavoriteActivity"
            android:label="@string/app_name" />
              <activity
            android:name=".SoundsActivity"
            android:label="@string/app_name" />
              <activity
            android:name=".SearchActivity"
            android:label="@string/app_name" />
              <activity
            android:name=".CartActivity"
            android:label="@string/app_name" />
               <activity
            android:name=".ProductSearchActivity"
            android:label="@string/app_name" />
                <activity
            android:name=".ProductDetailActivity"
            android:label="@string/app_name" />
                 <activity
            android:name=".CheckoutActivity"
            android:label="@string/app_name" />
                  <activity
            android:name=".PaymentActivity"
            android:label="@string/app_name" />
                      <activity
            android:name=".AddJournalEntryActivity"
            android:label="@string/app_name" />
                      
                      <receiver android:name=".AlarmReceiver" />
                      
               
    </application>

</manifest>
